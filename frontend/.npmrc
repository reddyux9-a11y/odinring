# CRA 5 + eslint-config-react-app expect ESLint 8 peers; this repo uses ESLint 9 for flat config.
# npm 11 + npm ci otherwise fails peer resolution. Vercel reads this for `npm ci`.
legacy-peer-deps=true
